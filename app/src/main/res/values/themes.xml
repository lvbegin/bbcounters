<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BbCounters" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="fontFamily">@font/oswald</item>
        <!-- Customize your theme here. -->
        <item name="android:navigationBarColor">
            @color/primaryColor
        </item>

    </style>

    <style name="dialog_with_rounded_corner" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/dialog_rounded</item>
    </style>

    <style name="Theme.BbCounters.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.BbCounters.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BbCounters.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>